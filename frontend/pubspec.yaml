name: mindmate
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  flutter_background_service: ^5.0.0
  flutter_background_service_android: ^6.0.0
  geolocator: ^10.1.0      # 🟢 NEW: For Location Access
  # 🟢 NEW: For Voice Mode
  record: ^5.1.0           # To record audio
  path_provider: ^2.1.2    # To store temp audio files
  permission_handler: ^11.3.0 # To ask for microphone permission
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# Linux fix (safe to keep)
dependency_overrides:
  record_linux: ^1.2.0

flutter:
  uses-material-design: true